api_platform:
    title: Appbeta API
    version: '%app.version%'
    eager_loading:
        max_joins: 80
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
